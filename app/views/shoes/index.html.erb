<h1>All the Kicks</h1>

<% @shoes.each do |s| %>

  <h2><%= link_to s.name, shoe_path(s.id)%> - <%= s.name %> </h2>
  <%= link_to "Write a review", new_shoe_review_path%>
   <% end %>
  <div>
    <p><%= s.description %></p>
  </div>
